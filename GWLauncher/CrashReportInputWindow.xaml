<Window x:Class="BravoGameLauncherGui.CrashReportInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="크래시 상황 입력"
        Height="280"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="어떤 상황에서 크래시가 발생했는지 적어주세요."
                   FontWeight="SemiBold"
                   Margin="0,0,0,4"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="TxtBuildInfo"
                   Grid.Row="1"
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"/>

        <TextBox x:Name="TbReport"
                 Grid.Row="2"
                 MaxLength="100"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 VerticalContentAlignment="Top"
                 TextChanged="TbReport_TextChanged"/>

        <TextBlock x:Name="TxtCounter"
                   Grid.Row="3"
                   HorizontalAlignment="Right"
                   FontSize="12"
                   Foreground="Gray"
                   Margin="0,4,2,0"
                   Text="0 / 100"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button x:Name="BtnCancel"
                    Content="취소"
                    Width="80"
                    Height="26"
                    Margin="0,0,8,0"
                    IsCancel="True"/>
            <Button x:Name="BtnConfirm"
                    Content="확인"
                    Width="80"
                    Height="26"
                    IsEnabled="False"
                    Click="BtnConfirm_Click"/>
        </StackPanel>
    </Grid>
</Window>

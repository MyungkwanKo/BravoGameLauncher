<Window x:Class="BravoGameLauncherGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BravoGameLauncherGui"
        Title="{x:Static local:LauncherVersionInfo.WindowTitle}"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 상단 공통 헤더: 메뉴 + 위키 링크(항상 고정) -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 상단 메뉴 (공통) -->
            <Menu Grid.Row="0">
                <MenuItem Header="파일(_F)">
                    <MenuItem Header="런처 저장 경로 바로가기" Click="MenuOpenLauncherFolder_Click" />
                    <MenuItem Header="종료(_X)" Click="MenuExit_Click" />
                </MenuItem>
            </Menu>

            <!-- 상단 고정 Wiki 링크 -->
            <Border Grid.Row="1"
                    Padding="10,6"
                    BorderBrush="#DDDDDD"
                    BorderThickness="0,0,0,1"
                    Background="Transparent">
                <TextBlock VerticalAlignment="Center">
                    <Hyperlink NavigateUri="https://krafton.atlassian.net/wiki/spaces/PROJECTGW/pages/846770135/GW+Launcher"
                            RequestNavigate="Hyperlink_RequestNavigate">
                        [통합런처 사용 가이드 wiki 바로가기]
                    </Hyperlink>
                </TextBlock>
            </Border>
        </Grid>

        <!-- 좌측 탭 메뉴 -->
        <TabControl Grid.Row="1"
                    TabStripPlacement="Left"
                    SelectedIndex="2"
                    Margin="0"
                    SelectionChanged="MainTab_SelectionChanged">

            <!-- 0) Engine -->
            <TabItem Header="Engine">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- UE 버전 선택 -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="UE Version:" Width="90" VerticalAlignment="Center"/>
                            <ComboBox x:Name="CbEngineVersion"
                                    Width="160"
                                    Height="24"
                                    VerticalContentAlignment="Center"
                                    SelectionChanged="CbEngineVersion_SelectionChanged"/>
                        </StackPanel>

                        <!-- 설치 경로 -->
                        <GroupBox Header="Installed Build 설치 경로" Grid.Row="1" Margin="0,0,0,8">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="BasePath:" Width="90" VerticalAlignment="Center"/>
                                    <TextBox x:Name="TbEngineBasePath" Width="520" Height="24" IsReadOnly="True" VerticalContentAlignment="Center"/>
                                    <Button x:Name="BtnEngineChangePath" Content="변경..." Width="80" Height="24" Margin="8,0,0,0"
                                            Click="BtnEngineChangePath_Click"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal">
                                    <TextBlock Text="InstallRoot:" Width="90" VerticalAlignment="Center"/>
                                    <TextBox x:Name="TbEngineInstallRoot" Width="610" Height="24" IsReadOnly="True" VerticalContentAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>

                        <!-- 서버/로컬 상태 -->
                        <Grid Grid.Row="2" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <GroupBox Header="서버 최신 Installed Build" Grid.Column="0" Margin="0,0,6,0">
                                <Grid Margin="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock x:Name="TxtEngineServerInfo" TextWrapping="Wrap" />
                                    <Button x:Name="BtnEngineRefresh" Grid.Row="1" Content="새로고침" Width="100" Height="26" Margin="0,8,0,0"
                                            Click="BtnEngineRefresh_Click"/>
                                </Grid>
                            </GroupBox>

                            <GroupBox Header="로컬 Installed Build 상태" Grid.Column="1" Margin="6,0,0,0">
                                <Grid Margin="10">
                                    <TextBlock x:Name="TxtEngineLocalInfo" TextWrapping="Wrap"/>
                                </Grid>
                            </GroupBox>
                        </Grid>

                        <!-- 작업 버튼 + 진행 -->
                        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button x:Name="BtnEngineDownload" Content="다운로드" Width="110" Height="28"
                                    Click="BtnEngineDownload_Click"/>
                            <Button x:Name="BtnEngineDownloadInstall" Content="다운로드 + 설치" Width="140" Height="28" Margin="8,0,0,0"
                                    Click="BtnEngineDownloadInstall_Click"/>
                            <ProgressBar x:Name="PbEngine" Width="240" Height="18" Margin="16,4,0,0"/>
                            <TextBlock x:Name="TxtEngineProgress" Margin="8,4,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <!-- 로그 -->
                        <TextBox x:Name="TxtEngineLog"
                                Grid.Row="4"
                                IsReadOnly="True"
                                TextWrapping="Wrap"
                                VerticalScrollBarVisibility="Auto"
                                HorizontalScrollBarVisibility="Auto"
                                AcceptsReturn="True"/>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- 1) Setup_p4 -->
            <TabItem Header="Setup p4">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Perforce 환경변수 설정 (setup_p4)"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,8"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Workspace(P4CLIENT):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="TbP4Workspace"
                                 Width="260"
                                 Height="24"
                                 VerticalContentAlignment="Center"
                                 Margin="0,0,8,0"/>
                        <Button x:Name="BtnSetupP4Apply"
                                Content="적용"
                                Width="80"
                                Height="24"
                                Click="BtnSetupP4Apply_Click"/>
                    </StackPanel>

                    <!-- P4USER 선택 (단일 선택 체크박스) -->
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="P4USER:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <CheckBox x:Name="CbP4UserDeveloper"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0"
                                Click="CbP4UserDeveloper_Click">
                            <TextBlock Text="gw_developer"/>
                        </CheckBox>

                        <CheckBox x:Name="CbP4UserEngine"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0"
                                Click="CbP4UserEngine_Click">
                            <TextBlock Text="gw_engine"/>
                        </CheckBox>

                        <CheckBox x:Name="CbP4UserGuest"
                                VerticalAlignment="Center"
                                Margin="0,0,10,0"
                                Click="CbP4UserGuest_Click">
                            <TextBlock Text="gw_guest"/>
                        </CheckBox>
                        </StackPanel>

                    <!-- 탭별 로그: Setup p4 -->
                    <TextBox x:Name="TxtSetupP4Log"
                             Grid.Row="3"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             AcceptsReturn="True"/>
                </Grid>
            </TabItem>

            <!-- 2) GWEditor (통합: p4 sync 포함) -->
            <TabItem Header="GWEditor">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- 상단: WS/Project/Editor + Local CL/Build CL + Sync 필요여부 | 구분선 | 버튼(2x2) -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 행 단위 줄맞춤: 라벨(0열) | 내용(1열) -->
                        <Grid.RowDefinitions>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="28"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Workspace(P4CLIENT)" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox  Grid.Row="0" Grid.Column="1" x:Name="TbGWEditorWorkspace" IsReadOnly="True" Height="24" VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Project (.uproject)" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox  Grid.Row="1" Grid.Column="1" x:Name="TbGWEditorProjectPath" IsReadOnly="True" Height="24" VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Editor (UnrealEditor.exe)" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox  Grid.Row="2" Grid.Column="1" x:Name="TbGWEditorEditorExe" IsReadOnly="True" Height="24" VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Local CL" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox  Grid.Row="3" Grid.Column="1" x:Name="TbGWEditorLocalCL" IsReadOnly="True" Height="24" VerticalAlignment="Center"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="GW_ProjectBuild CL" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox  Grid.Row="4" Grid.Column="1" x:Name="TbGWEditorBuildCL" IsReadOnly="True" Height="24" VerticalAlignment="Center"/>

                        <!-- Sync 필요여부: 이름칸에 라벨+상태 아이콘, 내용칸은 메시지만(다른 박스와 동일 너비) -->
                        <StackPanel Grid.Row="5" Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Sync 필요여부" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Ellipse x:Name="GWEditorSyncStatusIndicator" Width="12" Height="12"
                                     VerticalAlignment="Center" Fill="Gray"/>
                        </StackPanel>
                        <TextBox Grid.Row="5" Grid.Column="1" x:Name="TbGWEditorSyncStatus"
                            IsReadOnly="True" MinHeight="24" Height="40" VerticalAlignment="Center"
                            TextWrapping="Wrap" VerticalContentAlignment="Top" Margin="0,0,0,0"/>

                        <!-- 구분선 + 실행 버튼 2x2 -->
                        <Border Grid.Row="0" Grid.RowSpan="6" Grid.Column="2"
                                BorderBrush="#DDDDDD" BorderThickness="1,0,0,0" Margin="12,0,0,0" Padding="12,0,0,0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <StackPanel Grid.Row="0" Orientation="Horizontal">
                                    <Button x:Name="BtnGWEditorRefresh" Content="새로고침" Width="90" Margin="0,0,8,0"
                                            Click="BtnGWEditorRefresh_Click"/>
                                    <Button x:Name="BtnRunGWEditor" Content="Editor실행" Width="90"
                                            Click="BtnRunGWEditor_Click"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,6,0,0">
                                    <Button x:Name="BtnGWEditorSync" Content="Sync" Width="90" Margin="0,0,8,0"
                                            Click="BtnGWEditorSync_Click"/>
                                    <Button x:Name="BtnGWEditorLocalRollback" Content="Local Rollback" Width="110"
                                            Click="BtnGWEditorLocalRollback_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- 실행 옵션 (에디터 실행 인자) -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="실행 옵션:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="TbGWEditorArgs" Height="24" VerticalContentAlignment="Center"
                                 Text="-nocompile" MinWidth="280"/>
                        <Button x:Name="BtnGWEditorArgsReset" Content="Reset" Width="60" Height="24" Margin="8,0,0,0"
                                Click="BtnGWEditorArgsReset_Click"/>
                    </StackPanel>

                    <!-- 탭별 로그 -->
                    <TextBox x:Name="TxtGWEditorLog"
                            Grid.Row="2"
                            IsReadOnly="True"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto"
                            HorizontalScrollBarVisibility="Auto"
                            AcceptsReturn="True"/>
                </Grid>
            </TabItem>


            <!-- 3) GameStarter (v2 UI 그대로 유지) -->
            <TabItem Header="GameStarter">
                <Grid Margin="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="2*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- 상단 컨트롤 -->
                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="10,8,10,4">
                        <TextBlock Text="빌드 타입:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="CmbBuildType"
                                  Width="130"
                                  SelectedIndex="0"
                                  VerticalAlignment="Center">
                            <ComboBoxItem Content="All" />
                            <ComboBoxItem Content="Development" />
                            <ComboBoxItem Content="Shipping" />
                        </ComboBox>

                        <TextBlock Width="20" />

                        <Button x:Name="BtnRefreshFromServer"
                                Content="빌드목록 새로고침"
                                Width="140"
                                Margin="0,0,8,0"
                                Click="BtnRefreshFromServer_Click" />

                        <Button x:Name="BtnRun"
                                Content="게임 실행"
                                Width="80"
                                Margin="0,0,8,0"
                                Click="BtnRun_Click" />

                        <CheckBox x:Name="CbWindowed"
                                  Content="창모드"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0" />

                        <!-- 구분선 -->
                        <TextBlock Text="│"
                                VerticalAlignment="Center"
                                Margin="4,0,4,0" />

                        <!-- 실행 대상: 클라이언트 / DS -->
                        <CheckBox x:Name="CbRunClient"
                                  Content="클라이언트"
                                  IsChecked="True"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0" />
                        <CheckBox x:Name="CbRunDS"
                                  Content="DS"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0" />
                    </StackPanel>

                    <!-- 다운로드/설치 진행율 (Engine 탭과 동일한 방식) -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0,10,4">
                        <TextBlock Text="진행율:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ProgressBar x:Name="PbGameStarter" Width="240" Height="18" Margin="0,0,8,0"/>
                        <TextBlock x:Name="TxtGameStarterProgress" VerticalAlignment="Center" MinWidth="120"/>
                    </StackPanel>

                    <!-- 클라이언트 실행 옵션 (GW.exe 인자): Grid로 남은 폭만 사용, 긴 줄은 입력란 내 가로 스크롤 -->
                    <Grid Grid.Row="2" Margin="10,0,10,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" MinWidth="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="클라이언트 실행 옵션:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Grid.Column="1" x:Name="TbGameStarterArgs" Height="24" VerticalContentAlignment="Center"
                                 MinWidth="0"
                                 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"/>
                        <Button Grid.Column="2" x:Name="BtnGameStarterArgsReset" Content="Reset" Width="60" Height="24" Margin="8,0,0,0"
                                Click="BtnGameStarterArgsReset_Click"/>
                    </Grid>

                    <!-- DS 실행 옵션 (GWServer.exe 인자): 2줄 높이, 줄바꿈 표시 -->
                    <Grid Grid.Row="3" Margin="10,0,10,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*" MinWidth="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="DS 실행 옵션:" VerticalAlignment="Top" Margin="0,4,8,0"/>
                        <TextBox Grid.Column="1" x:Name="TbGameStarterDsArgs" MinHeight="48" Height="48" MinWidth="0"
                                 VerticalContentAlignment="Top"
                                 Text="/GWBattleRoyale/Maps/L_BR_Proto?port=7778 -log -trace=cpu,frame,net,bookmark,stats -statnamedevents -tracefile -NetTrace=1"
                                 TextWrapping="Wrap" AcceptsReturn="False"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"/>
                        <Button Grid.Column="2" x:Name="BtnGameStarterDsArgsReset" Content="Reset" Width="60" Height="24" Margin="8,4,0,0"
                                VerticalAlignment="Top" Click="BtnGameStarterDsArgsReset_Click"/>
                    </Grid>

                    <!-- 캐시 경로 -->
                    <StackPanel Orientation="Horizontal" Grid.Row="4" Margin="10,0,10,4">
                        <TextBlock Text="로컬 캐시 경로:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock x:Name="TxtCachePath"
                                   VerticalAlignment="Center"
                                   TextWrapping="Wrap" />
                        <Button x:Name="BtnChangeCachePath" Content="캐시 경로 변경" Width="120" Height="24" Margin="12,0,0,0"
                                Click="MenuChangeCachePath_Click"/>
                        <Button x:Name="BtnClearCache" Content="캐시 삭제" Width="90" Height="24" Margin="8,0,0,0"
                                Click="MenuClearCache_Click"/>
                        <Button x:Name="BtnOpenCachePath" Content="바로가기" Width="70" Height="24" Margin="8,0,0,0"
                                Click="BtnOpenCachePath_Click"/>
                    </StackPanel>

                    <!-- 빌드 리스트: 체크박스 선택 컬럼 + v2 최종 컬럼 (가로 스크롤바 비활성 → 하단 로그와 겹침 방지) -->
                    <ListView x:Name="LvBuilds"
                              Grid.Row="5"
                              Margin="10,0,10,4"
                              SelectionMode="Single"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.View>
                            <GridView>

                                <!-- v2 방식: 체크박스가 ListViewItem.IsSelected에 바인딩 -->
                                <GridViewColumn Width="40">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      IsChecked="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem},
                                                                          Path=IsSelected,
                                                                          Mode=TwoWay}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn Header="BuildNo."
                                                Width="80"
                                                DisplayMemberBinding="{Binding BuildNo}" />
                                <GridViewColumn Header="Version"
                                                Width="100"
                                                DisplayMemberBinding="{Binding Version}" />
                                <GridViewColumn Header="CL"
                                                Width="80"
                                                DisplayMemberBinding="{Binding CL}" />
                                <GridViewColumn Header="Config"
                                                Width="100"
                                                DisplayMemberBinding="{Binding Config}" />
                                <GridViewColumn Header="Date"
                                                Width="120"
                                                DisplayMemberBinding="{Binding BuildDate}" />
                                <GridViewColumn Header="Time"
                                                Width="100"
                                                DisplayMemberBinding="{Binding BuildTime}" />
                                <GridViewColumn Header="DS"
                                                Width="50"
                                                DisplayMemberBinding="{Binding DS}" />
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <!-- 로그: GameStarter -->
                    <TextBox x:Name="TxtLog"
                             Grid.Row="6"
                             Margin="10,0,10,10"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             AcceptsReturn="True" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
